import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

export function Navbar() {
  return (
    <View style={styles.container}>
      <Text></Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'rgba(107,2,254,1)',
    width: '100%',
    height: 60
  }
});
